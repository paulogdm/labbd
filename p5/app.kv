<MainApp>:
	BoxLayout:
		orientation: 'vertical'

		Label:
			text: "You are now connected"
			font_size: 32

		DropDown:
			id: dropdown
			on_parent: self.dismiss()

		Button:
			text: "Disconnect"
			font_size: 24
			on_press: root.disconnect()
